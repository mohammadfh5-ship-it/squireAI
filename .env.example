# Optional. Used only by `npm run dev` via /api/intent.
# Production/preview builds always use the keyword fallback (seed ranking is never invented).
OPENAI_API_KEY=
LLM_API_KEY=
LLM_MODEL=gpt-4o-mini
